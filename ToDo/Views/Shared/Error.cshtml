@model ErrorViewModel
@{
    ViewData["Title"] = "Ошибка";
}

<h1 class="text-danger">Ошибка.</h1>
<h2 class="text-danger">Произошла ошибка во время обработки вашего запроса.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID запроса:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Режим разработки</h3>
<p>
    Переключение в <strong>режим разработки</strong> позволит отображать более подробную информацию об ошибке.
</p>
<p>
    <strong>Режим разработки не должен быть включен для развернутых приложений.</strong>
    Это может привести к отображению чувствительной информации из исключений конечным пользователям.
    Для локальной отладки включите <strong>режим разработки</strong>, установив переменную среды <strong>ASPNETCORE_ENVIRONMENT</strong> в <strong>Development</strong>
    и перезапустите приложение.
</p>